<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="200dp"
    android:layout_height="100dp"
    android:layout_margin="7dp"
    android:background="@drawable/bgdetails"
    android:backgroundTint="@color/transparent_dark"
    app:cardCornerRadius="30dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="30dp"
        android:paddingTop="20dp"
        android:paddingEnd="30dp"
        android:paddingBottom="20dp">

        <TextView
            android:id="@+id/coin_name_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/mypink"
            android:textStyle="bold"
            tools:text="Coin Name" />

        <TextView
            android:id="@+id/coin_symbol_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/coin_name_text_view"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/myblue"
            android:textSize="12sp"
            tools:text="Symbol - (AAA)" />

        <TextView
            android:id="@+id/coin_rank_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/coin_symbol_text_view"
            android:textColor="@color/white"
            android:textSize="12sp"
            app:drawableEndCompat="@drawable/star"
            tools:text="Rank - 73" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>